const User = require('../models/User')
const Admin = require('../models/Admin')
const Costs = require('../models/Costs')
const Job = require('../models/Job')

module.exports = {User, Admin, Job, Costs}